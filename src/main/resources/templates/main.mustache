<!DOCTYPE HTML>
<html lang="ru">
<head>
    <title>Web Application - Main Page</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
</head>
<style type="text/css">
    h1 {
        border-bottom: cadetblue 1px solid;
    }
    #greeting, #menu, #scenario-form, #scenario-list {
        padding: 10px 0px 10px 0px;
    }
    .description {
        color: cadetblue;
    }
    .list-item {
        padding: 5px 0px 5px 0px;
    }
    #logout {
        float: right;
    }
    #logout span {
        margin-right: 50px;
    }
</style>
<body>
<div>
    <div id="logout">
        <form action="/logout" method="POST">
            <span>Authenticated as <b>{{username}}</b></span>
            <input type="submit" value="Sign Out"/>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        </form>
    </div>
    <h1>Main Page</h1>
    <div id="greeting">
        Hello, {{username}}!
    </div>
    <div id="menu">
        <a href="/sysinfo">System Info</a>
    </div>
    <div id="scenario-form">
        <strong>New Scenario:</strong>
        <form action="/" method="POST">
            <input type="text" name="name" placeholder="Name:"/>
            <input type="text" name="descr" placeholder="Description:"/>
            <input type="submit" value="Add"/>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        </form>
    </div>
    <div id="scenario-list">
        <strong>Scenario List:</strong>
        <div id="scenario-list-filter">
            <form action="/filter" method="POST">
                Filter:
                <input type="text" name="filter" placeholder="Name of scenario"/>
                <input type="submit" value="Find"/>
                <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
            </form>
        </div>
        <ul>
            {{#scenarios}}
            <li class="list-item">
                <b>{{name}}</b> (ID: {{id}})<br />
                {{#createDateTime}}Created: {{createDateTime}}{{/createDateTime}}<br />
                {{#changeDateTime}}Changed: {{changeDateTime}}{{/changeDateTime}}<br />
                <span class="description">{{description}}</span>
            </li>
            {{/scenarios}}
        </ul>
    </div>
</div>
</body>
</html>